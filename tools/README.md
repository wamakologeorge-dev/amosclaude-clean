# Amosclaud Tools

Executable host tools are exposed through the `amosclaud-memory`, `amosclaud-workspace`, `amosclaud-runner`, and `amcl` console commands. Agent-callable tools remain allowlisted in `amoscloud_ai/agent_actions.py`.
