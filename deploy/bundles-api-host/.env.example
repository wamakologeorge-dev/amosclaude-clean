# Dedicated credential recommended for bundle upload and download access.
AMOSCLAUD_BUNDLES_API_KEY=replace-with-a-long-random-key

# Persistent bundle storage and maximum upload size (default: 2 GiB).
AMOSCLAUD_BUNDLE_ROOT=/data/bundles
AMOSCLAUD_BUNDLE_MAX_BYTES=2147483648
PORT=8080
