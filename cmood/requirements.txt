# Legacy cmood compatibility adapter dependencies.
# Install the repository itself so cmood uses the same Amosclaud Autonomous
# kernel, shared contracts, repository APIs, credentials, and verification
# models as the rest of PR #434 rather than becoming an independent agent.
-e ..

fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.8,<3
python-dotenv>=1.0,<2
watchdog>=4,<7
httpx>=0.27,<1
