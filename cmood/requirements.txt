# Legacy cmood compatibility adapter dependencies.
# Install the parent Amosclaud repository in editable mode so cmood always uses
# the unified local Amosclaud packages during development and CI.

-e ..
fastapi>=0.139.2,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.8,<3
python-dotenv>=1.0,<2
watchdog>=4,<7
httpx>=0.27,<1
