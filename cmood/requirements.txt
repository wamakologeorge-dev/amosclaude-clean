# Legacy cmood compatibility adapter dependencies.
# The workflow installs the Amosclaud repository itself explicitly before
# installing these adapter dependencies, so this file contains only cmood's
# direct runtime requirements.

fastapi>=0.139.2,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.8,<3
python-dotenv>=1.0,<2
watchdog>=4,<7
httpx>=0.27,<1
