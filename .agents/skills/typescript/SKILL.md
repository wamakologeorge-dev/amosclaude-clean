---
name: typescript
description: "Typescript for amosclaude-clean. 4 gotchas, 5 conventions."
domain: typescript
triggers:
  - glob: "**/*.ts"
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript

Auto-compiled from **9 real patterns** in **amosclaude-clean**. This skill is auto-routed to agents when working on typescript files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

| ❌ Don't | Details |
|----------|----------|
| Don't use "any" type — define proper types/interfaces | any disables type checking entirely, hiding runtime errors |
| Handle Promise rejections — always .catch() or try/catch wit | Unhandled rejections crash Node processes or cause silent failures |
| Use === not == — strict equality prevents type coercion bugs | 0 == "0" is true; 0 === "0" is false |
| Don't use "as" casting to bypass type errors — fix the types | Type assertions hide real type mismatches |

## 📐 Conventions & Best Practices

### Compiled Rules
- 📐 **Always:** Define return types explicitly on public functions
  - _Prevents accidental return type widening_
- 📐 **Always:** Use unknown instead of any for external data
  - _Forces validation before use, unlike any_
- 📐 **Always:** Prefer interface over type for object shapes
  - _Interfaces support declaration merging; types are better for unions_
- 📐 **Always:** Mark readonly on properties that should not be mutated
  - _Compiler enforces immutability_
- ✅ **Always:** Use satisfies operator instead of as for type validation
  - _satisfies validates without widening the inferred type_

---
*Auto-generated by NeuroSync 🧠 | 9 patterns | 2026-08-03*
