---
name: python
description: "Python for amosclaude-clean. 8 gotchas, 6 conventions."
domain: python
triggers:
  - glob: "**/*.py"
  - glob: "**/requirements*.txt"
  - glob: "**/pyproject.toml"
autoGenerated: true
enabled: true
---

# Python

Auto-compiled from **19 real patterns** in **amosclaude-clean**. This skill is auto-routed to agents when working on python files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

| ❌ Don't | Details |
|----------|----------|
| Mutable default argument is shared across all calls | def f(x=[]) reuses the same list — use def f(x=None): x = x or [] instead |
| Late binding closures in loops capture variable by reference | lambda i=i: i captures the current value; without default, all lambdas share the final i |
| Circular imports cause ImportError or partially initialized  | Restructure to avoid import cycles — use local imports or dependency injection |
| Bare except: catches SystemExit and KeyboardInterrupt — use  | Bare except prevents Ctrl+C from working and swallows crashes silently |
| Mixing sync and async code without proper event loop managem | Never call asyncio.run() inside a running event loop — use await directly |
| os.path operations are not cross-platform — use pathlib.Path | pathlib handles path separators, joining, and resolution on all OSes |
| Never use pickle with untrusted data — it executes arbitrary | Deserializing a malicious pickle file gives the attacker code execution |
| Use secrets module for tokens and random IDs — not random mo | random is predictable; secrets.token_urlsafe() is cryptographically secure |

## 📐 Conventions & Best Practices

### Compiled Rules
- 📐 **Always:** Add type hints to all function signatures (PEP 484)
  - _Type hints enable mypy static analysis and improve editor autocomplete_
- 📐 **Always:** Use context managers (with) for file, DB connections, and locks
  - _Guarantees cleanup even if an exception occurs_
- 📐 **Always:** Use logging module, not print() for application output
  - _logging supports levels, handlers, formatting, and can be silenced in tests_
- 📐 **Always:** Use virtual environments — never install project deps globally
  - _Global installs cause version conflicts across projects_
- ✅ **Always:** Use dataclasses or Pydantic models for structured data
  - _Plain dicts have no schema — dataclasses add type safety and __repr___
- ✅ **Always:** Pin dependency versions in requirements.txt or pyproject.toml
  - _Unpinned versions break reproducibility when new releases ship breaking changes_

---
*Auto-generated by NeuroSync 🧠 | 19 patterns | 2026-08-03*
