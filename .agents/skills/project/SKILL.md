---
name: project
description: "Project for amosclaude-clean. 8 gotchas, 14 conventions."
domain: project
triggers:
  - glob: "**/*"
autoGenerated: true
enabled: true
---

# Project

Auto-compiled from **66 real patterns** in **amosclaude-clean**. This skill is auto-routed to agents when working on project files.

## ⚠️ Anti-Patterns & Gotchas

> **CRITICAL:** These are real gotchas from this project. Ignoring them WILL cause bugs.

| ❌ Don't | Details |
|----------|----------|
| Clean up effects — return cleanup function from useEffect | Not returning a cleanup causes memory leaks on route change |
| Don't create components inside other components | Causes remount on every render, destroying state and performance |
| Always use key prop when rendering lists | Use unique ID, not array index, to avoid reconciliation bugs |
| Don't call setState directly inside render or useEffect with | Causes infinite re-render loops |
| Don't mix CommonJS (require) and ESM (import) in same projec | Results in runtime module loading failures |
| Don't fetch data inside render loops or hot paths | Creates waterfall requests and degrades performance |
| Don't swallow errors silently — empty catch blocks hide bugs | Always log or re-throw errors |
| Don't prop-drill more than 2 levels — use Context or state m | Deep prop chains make refactoring painful and introduce bugs |

## 📐 Conventions & Best Practices

### Compiled Rules
- 📐 **Always:** Use const by default, let when reassignment needed, never var
  - _var has function scope and hoisting surprises_
- 📐 **Always:** Use optional chaining (?.) and nullish coalescing (??) for safe access
  - _Avoids TypeError on null/undefined chains_
- 📐 **Always:** Use template literals for string interpolation, not concatenation
  - _More readable and avoids accidental type coercion_
- 📐 **Always:** Prefer Array methods (map, filter, reduce) over manual loops
  - _More expressive and easier to chain_
- 📐 **Always:** Use useMemo for expensive computations, useCallback for stable references
  - _Prevents unnecessary recalculations and child re-renders_
- 📐 **Always:** Always add loading, error, and empty states for async operations
  - _Silent loading states confuse users_
- 📐 **Always:** Use pagination for lists over 100 items
  - _Rendering large flat lists blocks the main thread_
- 📐 **Always:** Debounce user input handlers (search, resize, scroll)
  - _Prevents excessive function calls on rapid input_
- 📐 **Always:** Disable submit button during form submission
  - _Prevents double-submit race conditions_
- ✅ **Always:** Enable strict mode in tsconfig.json
  - _Catches null/undefined errors and implicit any at compile time_
- ✅ **Always:** Close resources (streams, connections, handles) in finally blocks
  - _Prevents resource leaks on error paths_
- ✅ **Always:** Implement retry logic for transient failures with exponential backoff
  - _Network requests fail transiently; retries improve resilience_
- ✅ **Always:** Use Suspense and Error Boundaries for async operations
  - _Provides graceful fallback UI during loading and failure_
- ✅ **Always:** Implement soft delete for important data
  - _Hard deletes are irreversible; soft delete enables recovery_

---
*Auto-generated by NeuroSync 🧠 | 66 patterns | 2026-08-03*