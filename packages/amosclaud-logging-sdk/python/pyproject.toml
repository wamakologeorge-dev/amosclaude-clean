[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "amosclaud-logging"
version = "0.1.0"
description = "Python SDK for the self-hosted Amosclaud logging service"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27,<1"]
