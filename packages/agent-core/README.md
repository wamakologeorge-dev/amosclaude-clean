# Amosclaud Agent Core

The framework is implemented from scratch in `amoscloud_ai/engineering_agent.py`, `provider.py`, and `agent_actions.py`. This boundary owns planning, constrained tools, state, evidence, and verification. It does not own HTTP authentication or billing.
