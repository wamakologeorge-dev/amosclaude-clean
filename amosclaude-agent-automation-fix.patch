diff --git a/.github/workflows/deploy.yml b/.github/workflows/deploy.yml
index 7ade2d8..e22be40 100644
--- a/.github/workflows/deploy.yml
+++ b/.github/workflows/deploy.yml
@@ -1,56 +1,3 @@
-name: Deployment Pipeline
-
-on:
-  repository_dispatch:
-    types: [deploy]
-
-permissions:
-  contents: read
-
-jobs:
-  deploy:
-    runs-on: ubuntu-latest
-    steps:
-      - name: Checkout code
-        uses: actions/checkout@v3
-
-      - name: Report Queued
-        run: |
-          curl -X POST ${{ secrets.AMOSCLOUD_WEBHOOK_URL }} \
-            -H "Content-Type: application/json" \
-            -d '{"run_id": "${{ github.event.client_payload.run_id }}", "webhook_secret": "${{ github.event.client_payload.webhook_secret }}", "status": "building", "logs": "Deployment starting..."}' || true
-
-      - name: Simulate Build
-        run: |
-          echo "Building Amoscloud..."
-          sleep 2
-          curl -X POST ${{ secrets.AMOSCLOUD_WEBHOOK_URL }} \
-            -H "Content-Type: application/json" \
-            -d '{"run_id": "${{ github.event.client_payload.run_id }}", "webhook_secret": "${{ github.event.client_payload.webhook_secret }}", "status": "building", "logs": "Build in progress...\\n"}' || true
-          echo "Build successful."
-          sleep 2
-
-      - name: Simulate Deploy
-        run: |
-          echo "Deploying to ${{ github.event.client_payload.environment }}..."
-          sleep 2
-          curl -X POST ${{ secrets.AMOSCLOUD_WEBHOOK_URL }} \
-            -H "Content-Type: application/json" \
-            -d '{"run_id": "${{ github.event.client_payload.run_id }}", "webhook_secret": "${{ github.event.client_payload.webhook_secret }}", "status": "building", "logs": "Deploying to environment...\\n"}' || true
-
-      - name: Report Success
-        if: success()
-        run: |
-          curl -X POST ${{ secrets.AMOSCLOUD_WEBHOOK_URL }} \
-            -H "Content-Type: application/json" \
-            -d '{"run_id": "${{ github.event.client_payload.run_id }}", "webhook_secret": "${{ github.event.client_payload.webhook_secret }}", "status": "success", "logs": "Deployment completed successfully!\\n"}' || true
-
-      - name: Report Failure
-        if: failure()
-        run: |
-          curl -X POST ${{ secrets.AMOSCLOUD_WEBHOOK_URL }} \
-            -H "Content-Type: application/json" \
-            -d '{"run_id": "${{ github.event.client_payload.run_id }}", "webhook_secret": "${{ github.event.client_payload.webhook_secret }}", "status": "failed", "logs": "Deployment failed.\\n"}' || true
 name: CI/CD — Test & Deploy
 
 on:
@@ -58,32 +5,39 @@ on:
     branches: [main]
   pull_request:
     branches: [main]
+  workflow_dispatch:
+
+permissions:
+  contents: read
+
+concurrency:
+  group: deploy-${{ github.workflow }}-${{ github.ref }}
+  cancel-in-progress: true
 
 jobs:
   test:
     name: Run Tests
     runs-on: ubuntu-latest
-    permissions:
-      contents: read
     steps:
       - uses: actions/checkout@v7
       - name: Set up Python 3.11
         uses: actions/setup-python@v6
         with:
           python-version: "3.11"
+          cache: pip
       - name: Install dependencies
-        run: pip install -r requirements.txt
+        run: python -m pip install -r requirements.txt
       - name: Run tests
-        run: pytest tests/ -v --tb=short || echo "No tests found, skipping"
+        run: python -m pytest tests/ -v --tb=short
 
   deploy:
     name: Deploy to Render
     runs-on: ubuntu-latest
     needs: test
     permissions: {}
-    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
+    if: github.ref == 'refs/heads/main' && github.event_name == 'push' && secrets.RENDER_DEPLOY_HOOK_URL != ''
     steps:
-      - name: Trigger Render Deploy
-        run: |
-          curl -s -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}" \
-            -o /dev/null -w "HTTP %{http_code}\n"
+      - name: Trigger Render deploy
+        env:
+          RENDER_DEPLOY_HOOK_URL: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
+        run: curl --fail --silent --show-error --request POST "$RENDER_DEPLOY_HOOK_URL"
diff --git a/.github/workflows/environment.yml b/.github/workflows/environment.yml
deleted file mode 100644
index 6f5e4c7..0000000
--- a/.github/workflows/environment.yml
+++ /dev/null
@@ -1,25 +0,0 @@
-name: amosclaude
-channels:
-  - conda-forge
-  - defaults
-
-dependencies:
-  - python=3.10
-  - pip
-  - pytest
-  - flake8
-  - pip:
-      - fastapi
-      - uvicorn[standard]
-      - requests
-      - pyyaml
-      - python-dotenv
-      - click
-      - rich
-      - typer
-      - jinja2
-      - black
-      - isort
-      - pylint
-      - mypy
-      - bandit
diff --git a/.github/workflows/render.yaml b/.github/workflows/render.yaml
deleted file mode 100644
index 6c4d463..0000000
--- a/.github/workflows/render.yaml
+++ /dev/null
@@ -1,14 +0,0 @@
-# render.yaml
-services:
-  - type: web
-    name: amosclaud-platform
-    script: python main.py
-    env:
-      - key: FLASK_APP
-        value: main.py
-      - key: FLASK_ENV
-        value: production
-    ports:
-      - port: 3000
-        protocol: http
-
diff --git a/.github/workflows/server-release.yml b/.github/workflows/server-release.yml
index 9e317f3..a007aa5 100644
--- a/.github/workflows/server-release.yml
+++ b/.github/workflows/server-release.yml
@@ -1,10 +1,3 @@
-name: Portable Agent Server Release
-
-on:
-  workflow_dispatch:
-  push:
-    tags:
-      - "server-v*"
 name: Amosclaud Server Package
 
 on:
@@ -24,11 +17,6 @@ permissions:
   contents: write
 
 jobs:
-  verify-and-package:
-    runs-on: ubuntu-latest
-    steps:
-      - uses: actions/checkout@v4
-
   package:
     runs-on: ubuntu-latest
     steps:
diff --git a/amoscloud_ai/api/routes/agent.py b/amoscloud_ai/api/routes/agent.py
index 81b5111..4eaedb9 100644
--- a/amoscloud_ai/api/routes/agent.py
+++ b/amoscloud_ai/api/routes/agent.py
@@ -24,7 +24,7 @@ router = APIRouter(prefix="/agent", tags=["autonomous-runtime"])
 
 AGENT_NAME = "Amosclaud Autonomous Runtime"
 AGENT_OWNER = "Amosclaud"
-AGENT_ROLE = "autonomous build, deployment, and monitoring runtime"
+AGENT_ROLE = "autonomous build, deployment, and monitoring server"
 AGENT_HOME = "amosclaud.com"
 AGENT_PIPELINE = "Amosclaud autonomous pipeline"
 AGENT_MODE = "autonomous"
