# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# IDEs
.idea/
.vscode/

# Environment and secret files
.env
.env.*
!.env.example
!.env.production.example
!.env.workspace.example
*.pem
*.key
*.p12
*.pfx
secrets/*
!secrets/README.md

# Generated release and conversation packages
/amosclaud_autonomous_conversation_package.zip

# Local databases and runtime data
*.db
*.sqlite
*.sqlite3
data/*
!data/.gitkeep

# OS
.DS_Store
Thumbs.db

# NeuroSync — auto-generated / private (do not commit)
# To share a file with your team: remove its line from this block in .gitignore
#
# ── Always private (never commit) ─────────────────────────────────────────
.neurosync/.context-key
.neurosync/store.json
.neurosync/rules/
.neurosync/agent-rules.md
.agent-mem/
#
# ── Shareable with team (remove line to commit) ────────────────────────────
# ip_rules.md — your architecture constraints for all AI agents
.neurosync/ip_rules.md
#
# ── Auto-generated AI context (remove line to share with team) ─────────────
# These regenerate on every sync — commit to share AI context across teammates
CLAUDE.md
AGENT.md
.cursorrules
.windsurfrules
