# cmood workflow repair

The cmood workflow now uses pinned GitHub Actions, installs the compatibility adapter from its own directory, verifies `cmood.agent` imports successfully, and runs with an explicit timeout and least-privilege permissions.
