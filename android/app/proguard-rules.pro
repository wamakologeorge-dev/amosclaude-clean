# Project-specific R8/ProGuard rules.
# API responses are parsed into maps; no app-specific keep rules are currently required.
# Add rules here only when introducing reflection-dependent libraries or APIs.
