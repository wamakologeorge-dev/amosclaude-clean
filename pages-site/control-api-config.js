(() => {
  "use strict";

  // Public configuration only. No token, key, or credential belongs in this file.
  // The backend authenticates users with an HttpOnly secure session cookie.
  window.AMOSCLAUD_CONTROL_API = "https://www.amosclaud.com";
})();
